import { Scene } from "../core/Scene";
declare class PLYLoader {
    static SH_C0: number;
    static LoadAsync(url: string, scene: Scene, onProgress?: (progress: number) => void, format?: string): Promise<void>;
    static LoadFromFileAsync(file: File, scene: Scene, onProgress?: (progress: number) => void, format?: string): Promise<void>;
    private static _ParsePLYBuffer;
}
export { PLYLoader };
