import type { Scene } from "../core/Scene";
declare class PLYLoader {
    static LoadAsync(url: string, scene: Scene, onProgress?: (progress: number) => void): Promise<void>;
    static LoadFromFileAsync(file: File, scene: Scene, onProgress?: (progress: number) => void): Promise<void>;
}
export { PLYLoader };
