import { Scene } from "../../../core/Scene";
declare class SplatTexture {
    data: Uint32Array;
    width: number;
    height: number;
    constructor(data: Uint32Array, width: number, height: number);
    static FromScene(scene: Scene): SplatTexture;
}
export { SplatTexture };
