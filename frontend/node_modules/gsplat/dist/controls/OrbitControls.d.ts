import type { Camera } from "../cameras/Camera";
declare class OrbitControls {
    minAngle: number;
    maxAngle: number;
    minZoom: number;
    maxZoom: number;
    orbitSpeed: number;
    panSpeed: number;
    zoomSpeed: number;
    dampening: number;
    update: () => void;
    dispose: () => void;
    constructor(camera: Camera, domElement: HTMLElement, alpha?: number, beta?: number, radius?: number, enableKeyboardControls?: boolean);
}
export { OrbitControls };
